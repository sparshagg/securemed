# .codex

This folder contains reusable workflows and skills for AI agents working on SecureMed.

- `skills/` contains focused reusable instructions in `.codex/skills/<skill-name>/SKILL.md` format.
- `workflows/` contains repeatable step-by-step operating procedures.

Agents may create new skills/workflows when they discover repeated work patterns, but they must avoid duplicating existing instructions.
